// 1. Write a function that takes an integer in minutes and converts it to seconds.
// return the result.

function convertMinutesToSeconds(minutes) {
  const seconds = minutes * 60;
  return seconds;
}

const totalSeconds = convertMinutesToSeconds(15);
console.log(totalSeconds);

// 2. Write a function that takes an array of numbers and returns the sum of all even numbers.
const numbers = [2, 3, 9, 5, 4]
function totalAllEvenNumbers(totalAllEvenNumbers){
  if()
}
console.log(numbers);

// 3. Using Firestore, create a collection of your favorite childhood toys/games (at least 5).